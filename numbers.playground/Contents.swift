//: Playground - noun: a place where people can play

import UIKit

let language = "Swift"

let year: Int = 2014 // Int or Integer - used for whole numbers - constant because the year of the release will never change, would be an implicit Int if not specified

var version: Float = 1.0 // Double - because there are decimal digits, would be an implicit double if not specified
// double equals 15 decimals
// float equals 7 decimals

var isAwesome: Bool = true // Boolean or Bool (in swift) - which is a binary value; true or false